[build-system]
requires = ["setuptools>=64", "wheel", "Cython>=0.29", "numpy>=1.21"]
build-backend = "setuptools.build_meta"

[project]
name = "Renyi-to-ROC"
version = "0.1.0"
description = "Renyi to ROC utilities"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["numpy>=1.21"]

